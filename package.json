{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "Your project description",
  "main": "index.js",
  "scripts": {
    "psql": "docker run --rm -it --network=host --entrypoint=psql timescale/timescaledb-ha:pg16 postgresql://example:example2password@localhost:42345/example",
    "sqlchat": "docker run --rm -it --env-file .sqlchat.env -p 3123:3000 sqlchat/sqlchat"
  },
  "dependencies": {
    "ai": "^3.4.16"
  }
}
